# Kế hoạch nâng cấp toàn bộ UI Admin

## 1. Sửa lỗi 404 - Sửa sai đường dẫn trong AppLauncher
**File:** `src/components/features/dashboard/admin/AppLauncher.tsx`
- Sửa `/exercises/list` → `/exercises`
- Sửa `/dishes/list` → `/dishes`  
- Sửa `/ingredients/list` → `/ingredients`
- Xóa card "Cài đặt hệ thống" (trỏ đến `/admin/settings` không tồn tại)

## 2. Đồng bộ màu sắc Admin UI - Bỏ earth-tone, dùng admin theme tokens
Các file cần sửa:

### a. `src/components/features/user/UserList.tsx`
- Thay `text-earth` → `text-foreground`
- Thay `<Card>` wrapper → dùng admin-styled container (rounded-2xl border-border bg-card/50 backdrop-blur)
- Bỏ `<TypographyH3>` dùng `font-display text-2xl font-bold`

### b. `src/components/features/user/UserActions.tsx`
- Thay `border-sand` → `border-border`
- Thay `bg-earth text-cream hover:bg-clay` → `bg-primary text-primary-foreground hover:bg-primary/90`
- Thay class `rounded-full` → giữ `rounded-xl` cho phong cách admin
- Bỏ `coreformDialogContentClass` → dùng default Dialog styling (admin theme sẽ xử lý)

### c. `src/components/features/user/UserSearchForm.tsx`
- Di chuyển nút "Tìm kiếm" lên cùng dòng với input (layout ngang gọn hơn)
- Thêm styling phù hợp admin: `rounded-xl` cho input, button dùng admin style

### d. `src/components/features/dashboard/admin/AdminDashboard.tsx`
- Hero banner: Dùng CSS variables thay vì hardcode `#0ea5e9` → dùng `bg-gradient-to-r from-primary via-primary/80 to-primary/60`
- Hover gradient overlay: dùng primary color

### e. `src/components/features/dashboard/admin/UserGrowthChart.tsx` & `UserGoalChart.tsx`
- Thay hardcoded colors (`#e5e5e5`, `#0a0a0a`, `#737373`) bằng CSS variable-based classes (`border-border`, `text-foreground`, `text-muted-foreground`)
- Card borders: `border-[#e5e5e5]` → `border-border`

### f. `src/components/features/dashboard/admin/StatsCards.tsx`
- Already uses StatCard with admin colors → OK, giữ nguyên

### g. `src/components/features/dashboard/admin/AppLauncher.tsx`
- Thay hardcoded colors (`#e5e5e5`, `#0ea5e5`, `#171717`, `#737373`, `#a3a3a3`) bằng Tailwind CSS variable classes (`border-border`, `bg-card`, `text-foreground`, `text-muted-foreground`)
- Hover border: `hover:border-primary/40`

### h. `src/components/features/exercises/AdminExerciseCard.tsx` & `src/components/features/dishes/AdminDishCard.tsx`
- Thay toàn bộ earth-tone (`earth`, `clay`, `sand`, `cream`) bằng admin tokens (`foreground`, `primary`, `border`, `card`, `muted`, `muted-foreground`)

## 3. Nâng cấp trang Quản lý người dùng chuyên nghiệp hơn

### a. Thêm UserStatsBar vào UserList
- 4 thẻ thống kê nhỏ ở đầu: Tổng người dùng, Đang hoạt động, Đã khóa, Admin
- Dùng style glassmorphism phù hợp admin theme

### b. Cải tiến bảng UserList
- Cột Avatar: hiển thị lớn hơn, đẹp hơn với ring color theo role
- Cột Role: Thay Badge generic → Badge color theo role (ADMIN=violet, USER=blue)
- Cột Trạng thái: Dùng pill badge có dot indicator (● xanh=hoạt động, ● đỏ=đã khóa)
- Cột Hành động: Thay nút bằng icon-only actions (Lock/Unlock icon) gọn gàng hơn

### c. Cải tiến UserActions dialog
- Dialog dùng admin styling (backdrop-blur, border-border)
- Mô tả rõ hơn cho hành động khóa/mở khóa

## 4. Sửa DataTable styling cho Admin
**File:** `src/components/shared/data-table/data-table.tsx` (thêm admin scoped styles vào `dashboard.css`)
- Thêm CSS: `.coreform-admin [data-slot="table"]` hover states → dùng `admin-nav-hover`
- Border: `border-border` thay vì `border-border` (đã OK)

## Tổng số file cần sửa: ~14 files
- `AppLauncher.tsx` - Fix routes + đồng bộ màu
- `AdminDashboard.tsx` - Đồng bộ màu hero
- `UserGrowthChart.tsx` - Đồng bộ màu
- `UserGoalChart.tsx` - Đồng bộ màu  
- `UserList.tsx` - Nâng cấp + đồng bộ màu + thêm stats
- `UserActions.tsx` - Đồng bộ màu
- `UserSearchForm.tsx` - Cải tiến layout + đồng bộ
- `AdminExerciseCard.tsx` - Đồng bộ màu
- `AdminDishCard.tsx` - Đồng bộ màu
- `AdminExerciseList.tsx` - Đồng bộ màu header
- `AdminDishesList.tsx` - Đồng bộ màu header
- `dashboard.css` - Thêm admin data-table hover styles
